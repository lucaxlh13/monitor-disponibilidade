# Monitor de Disponibilidade

Script em Python usado para verificar se os sites estão online.

## Como rodar

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python monitor.py
